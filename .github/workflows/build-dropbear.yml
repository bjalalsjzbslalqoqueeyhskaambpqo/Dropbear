name: Dropbear ARM64 Test

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

jobs:
  build-android-arm64:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Dropbear
      uses: actions/checkout@v4
      with:
        repository: 'mkj/dropbear'
        ref: 'DROPBEAR_2026.91'
        fetch-depth: 1

    - name: Setup NDK
      uses: nttld/setup-ndk@v1
      id: setup-ndk
      with:
        ndk-version: r27c

    - name: Inyectar localoptions.h
      run: |
        cat << 'EOF' > localoptions.h
        #define DROPBEAR_SVR_PASSWORD_AUTH 0
        #define DROPBEAR_CLI_PASSWORD_AUTH 0
        EOF

    - name: Compilar
      env:
        NDK_PATH: ${{ steps.setup-ndk.outputs.ndk-path }}
      run: |
        export TOOLCHAIN=$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64
        export CC=$TOOLCHAIN/bin/aarch64-linux-android21-clang
        export AR=$TOOLCHAIN/bin/llvm-ar
        export RANLIB=$TOOLCHAIN/bin/llvm-ranlib
        export STRIP=$TOOLCHAIN/bin/llvm-strip

        ./configure --host=aarch64-linux-android --disable-zlib
        make PROGRAMS="dropbear dbclient dropbearkey" -j$(nproc)
        $STRIP dropbearmulti
        mkdir -p output
        cp dropbearmulti output/dropbearmulti-arm64

    - name: Subir Binario
      uses: actions/upload-artifact@v4
      with:
        name: dropbear-arm64
        path: output/dropbearmulti-arm64
